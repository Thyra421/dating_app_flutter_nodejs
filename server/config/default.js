export const DEFAULT_SETTINGS = {
    notifications: true,
    appearOnRadar: true,
    trackPosition: true,
    language: "en-US",
    darkMode: true
}

export const DEFAULT_STEPS = {
    confirmMail: false,
    identity: false,
    gettingStarted: false
}